{
  "name": "@widget/embed",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:widget": "node scripts/build-widget.mjs",
    "build:chat-ui": "vite build",
    "build": "npm run build:widget && npm run build:chat-ui",
    "dev": "npm run build:widget && concurrently \"vite build --watch\" \"node scripts/build-widget.mjs --watch\""
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "concurrently": "^9.0.0",
    "esbuild": "^0.24.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
